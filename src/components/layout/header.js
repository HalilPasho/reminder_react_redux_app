import React from 'react';
import './header.css';

const Header = () => (
  <header className="header">
    <div className="header_title">
      Calendar App
    </div>
  </header>
);

export default Header;
